export enum UserCredentials {
    STANDARD_USER = 'standard_user',
    LOCKED_OUT_USER = 'locked_out_user',
}

export enum PasswordCredentials {
    VALID_PASSWORD = 'secret_sauce',
    INVALID_PASSWORD = 'invalid_password123'
}
